# 桌面页面可访问模板、设置存储和内置服务启动命令；Windows 的 URL 在绑定端口后精确授权。
[[permission]]
identifier = "desktop-commands"
description = "Use local templates, settings and start the bundled debug backend"
commands.allow = ["local_templates", "local_settings", "start_backend"]
