# 使用宿主机 Wayland 库，避免旧库缺少新 Mesa 所需符号而触发 EGL_BAD_ALLOC。
# appimagetool 从 Tauri 工作目录读取本文件；路径相对于最终 AppDir。
usr/lib/libwayland-*.so*
# 音频客户端与宿主 PulseAudio / PipeWire 配套，避免旧 libpulse 在定位后阻塞媒体时钟。
usr/lib/libpulse*.so*
