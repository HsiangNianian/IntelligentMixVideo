# API 服务地址；未配置或留空时默认 http://localhost:8000。
VITE_API_URL=http://localhost:8000

# 示例视频 HTTP(S) 直链，也支持 public 目录路径（例如 /preview.mp4）；留空使用内置示例。
# 当前预览截取到源视频第 13 秒，建议使用至少 14 秒的视频。
VITE_PREVIEW_VIDEO_URL=
